class Integraciones 
  def largo_list
    var una_list = list()
    una_list.size
  end
  
  def remover_todo
    var una_list = list(4, 5, 9, 0)
    var otra_list = list(5, 0)
    una_list.remove_all(otra_list)
    una_list
  end
end