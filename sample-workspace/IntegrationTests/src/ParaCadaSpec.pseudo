clase Observador
    atributo notificado := falso
    
	metodo notificar
	  fijar notificado := si
//	  este, fijar_notificado_en si
//	  este.fijar_notificado_en si
//	  este.fijar_notificado(si)
	fin
fin

clase Observable

  atributo observadores
 
  metodo notificar_a_todos
  	para cada observador en observadores notificar
  fin 
  
fin