clase Expresiones

  metodo expresiones_complejas
    variable x = 4 /*formatter FIXME*/ (x.z()).foo.bar.vaz(lista(x.k()), 4,(5 > 0),
    nuevo Literales())
    variable w =(incrementar x) >(decrementar x)
    variable z = 0
    variable k = 5 > 0
  fin

fin