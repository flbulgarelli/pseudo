clase TareaCompuesta(subtareas)
  metodo tiempo
    subtareas.sum(lambda(s) s.tiempo fin)

clase TareaSimple(tiempo)

clase Proyecto
  metodo tarea_critica
    tareas.maximo(lambda(t) t.tiempo fin) 

