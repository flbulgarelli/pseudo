clase CuentaBancaria
  atributo monto = 10

  metodo depositar(cantidad)
    incrementar monto con cantidad
  fin

  metodo extraer(cantidad)
    decrementar monto con cantidad
  fin

  metodo imprimir
  //    'Cuenta(' // ++ monto ++ ')' 
  fin

fin
 

