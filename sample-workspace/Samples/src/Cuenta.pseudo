class CuentaBancaria
  attr monto = 10

  def depositar(cantidad)
    incr monto by cantidad
  end

  def extraer(cantidad)
    decr monto by cantidad
  end

  def imprimir
  //    'Cuenta(' // ++ monto ++ ')' 
  end

end
 

